{
  "name": "workdash-monorepo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "build:packages": "pnpm --filter @repo/db build && pnpm --filter @repo/shared build"
    
  },
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.28.2",
  "devDependencies": {
    "turbo": "^2.8.1"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}
